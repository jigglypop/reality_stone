[package]
name = "clarus_core"
version = "0.3.0"
edition = "2021"
authors = ["CE Research Lab"]
description = "Canonical Clarus compute core for Rust/Python runtime kernels"
license = "MIT"

[lib]
name = "_rust"
crate-type = ["cdylib", "rlib"]

[features]
default = []
python = ["pyo3", "dep:numpy"]
cuda = ["dep:cudarc"]

[dependencies]
pyo3 = { version = "0.21.2", features = ["extension-module", "abi3-py310"], optional = true }
numpy = { version = "0.21", optional = true }
ndarray = "0.15"
rayon = "1.7"
serde = { version = "1.0", features = ["derive"] }
rand = "0.8"
rand_distr = "0.4"
rustfft = "6.1"
cudarc = { version = "0.19", features = ["driver", "nvrtc", "cuda-12080", "dynamic-loading"], default-features = false, optional = true }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
